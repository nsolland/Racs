# Public architecture note

Internal contract-reuse maps, ownership topology, portfolio dependency maps, and implementation sequencing are intentionally not published.

The public RACS interoperability contract is defined by `SPECIFICATION.md`, the normative files under `spec/`, and the corresponding public conformance vectors. Those artifacts are the public source of truth.
